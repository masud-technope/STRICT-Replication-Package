Puts item constant pool hash table hash table item param item constant pool hash table Item threshold old Capacity oldCapacity table length Item old Map oldMap table new Capacity newCapacity old Capacity oldCapacity Item new Map newMap Item new Capacity newCapacity threshold new Capacity newCapacity table new Map newMap old Capacity oldCapacity Item old Map oldMap Item hash Code hashCode FFFFFFF new Capacity newCapacity new Map newMap new Map newMap hash Code hashCode FFFFFFF table length table table