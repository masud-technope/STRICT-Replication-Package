 GlyphKey(String token, Font font, FontRenderContext context) {
    assert token != null;
    assert font != null;
    assert context != null;
    this.token = token;
    this.font = font;
    this.context = context;
}